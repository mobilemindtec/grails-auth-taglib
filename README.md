Grails Authentication Taglib

Usage

```
<auth:isAdmin>
    show admin code
</auth:isAdmin>

<auth:isRoot>
    show root code
</auth:isRoot>

<auth:isUser>
    show user code
</auth:isUser>
```

Change files package and configure to use roles of your project. Add service and taglib file in respective folders.
